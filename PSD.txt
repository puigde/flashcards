Balanceig de càrrega,Equilibrar al màxim la quantitat de treball de cada thread per a tenir un mínim "idle" time,equlibri treball,
T(1),Temps d'execució seqüencial,seq,
Fine grained vs. Coarse grained, fine grained dona més facilitat per a equilibrar el balanceig de càrrega però té més overhead i per Coarse Grained el contrari,overhead,
T(inf), temps d'execució del camí crític el qual determina el temps d'execució mínima del programa, camí crític,
Paralelisme, T(1)/T(inf),de petit a gran baixant,
Parallel fraction, phi= % de temps a paralelitzar T(paralelitzable)/T(1),phi,
Speedup, T(1)/T(t), quant accelerem amb cert paralelisme t ,
Eficiència, Speedup(t)/t, ratio spt/t,
Llei the Amdhal, Speedup(inf)= 1/(1-phi), màxim speedup que podem aconseguir,
